Para usar el módulo sage con la clase Estadin,
en una ipynb sage, cargar el archivo que la contiene:

load("statinf.sage")

Luego puedo continuar con:
ces = Estadin()

Y utilizar el objeto en la hoja de trabajo.
